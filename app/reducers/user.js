// import * as types from '../actions/types';

const defaultState = {
    authenticated: false
};

const user = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default user;
